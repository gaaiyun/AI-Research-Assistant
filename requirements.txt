# AI Research Assistant — 依赖
# 安装：pip install -r requirements.txt

# ── 核心 ────────────────────────────────────────────────
# arXiv 客户端
arxiv>=2.0.0

# Semantic Scholar / HTTP
requests>=2.31.0

# 数据处理
pandas>=2.0.0
numpy>=1.24.0

# 向量检索（默认 TF-IDF 后端）
scikit-learn>=1.3.0

# PDF 解析
PyPDF2>=3.0.0

# ── UI / 可视化 ─────────────────────────────────────────
streamlit>=1.30.0
plotly>=5.18.0

# ── 测试 ────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0

# ── 工具 ────────────────────────────────────────────────
python-dateutil>=2.8.0

# ── 可选：高级向量检索 ─────────────────────────────────
# 装上后，vector_store.ChromaEmbeddingStore 才可用。
# 体积较大（torch + transformers，约 1.5 GB），仅在需要语义检索时安装：
#   pip install sentence-transformers chromadb
# sentence-transformers>=2.5.0
# chromadb>=0.4.0
